#lanche = ('Hambúrger', 'Suco', 'Pizza', 'Pudim', 'Batata Frita')
# Tuplas são imutáveis
#print(lanche)

#for cont in range(0, len(lanche)):
#    print(f'Eu vou comer {lanche[cont]}')

#for comida in lanche:
#    print(f'Eu vou comer {comida}')

#for pos, comida in enumerate(lanche):
#    print(f'Eu vou comer {comida} na posição {pos}')

#print(sorted(lanche)) # Coloca em ordem

'''a = (2, 5, 4)
b = (5, 8, 1, 2)
c = a + b # Mostra o 'a' e o 'b' (ele não soma)
print(len(c)) # Mostra quantidade de elementos dentro da tuplas
print(c)
print(c.count(5)) # Mostra quantas vezes está aparecendo o número 5 dentro de 'c'
print(c.index(8)) # Mostra a posição do '8'
print(c.index(5, 1)) # Mostra as posições do '5' no caso o primeiro e o que aparecer após a posição '1' (deslocamento) '''

pessoa = ('Gustavo', 39, 'M', 99.98)
print(pessoa)

del(pessoa) # Deleta variável

